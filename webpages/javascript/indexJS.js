// Get page elements
const indexLoginButton = document.getElementById("indexLoginButton");
const indexSignupButton = document.getElementById("indexSignupButton");

// Add event listeners
indexLoginButton.addEventListener("click", goToLogin);
indexSignupButton.addEventListener("click", goToSignup);
